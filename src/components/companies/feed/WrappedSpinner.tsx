import LoadingSpinner from "@/components/ui/LoadingSpinner";

export default function WrappedSpinner() {
  return (
    <div className="flex justify-center mt-12">
      <LoadingSpinner />
    </div>
  )
}